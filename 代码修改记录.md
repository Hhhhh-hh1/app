# 代码修改记录

## 2026-06-17 20:30:33 +08:00

- 修改内容：从零创建 Android 图像画质分析工具工程，包含 Gradle 配置、Java + XML 主界面、图片选择与预览、图片解码降采样、清晰度/曝光/噪点/对比度/色偏评分算法、综合评分输出、单元测试、README、约束说明、AI 协作说明和验证证据模板。
- 涉及文件：`.gitignore`、`settings.gradle`、`build.gradle`、`gradle.properties`、`local.properties`、`gradlew.bat`、`gradlew`、`gradle/wrapper/*`、`app/build.gradle`、`app/src/main/**`、`app/src/test/**`、`README.md`、`约束与决策说明.md`、`AI协作说明.md`、`验证证据/README.md`、`验证证据/构建验证记录.md`。
- 修改原因或目标：完成“AI 图像识别与画质分析工具”作业的 Android APK 原型，支持本地图像加载、画质分析、评分解释和可提交文档材料。
- 验证结果或后续注意事项：已执行 `.\gradlew.bat testDebugUnitTest assembleDebug`，单元测试和 Debug APK 构建成功；APK 位置为 `app/build/outputs/apk/debug/app-debug.apk`。当前 `adb devices -l` 未识别到手机，后续需要在手机端确认 USB 调试授权、连接模式和数据线。

## 2026-06-18 10:48:00 +08:00

- 修改内容：重构 Android 图像画质分析 App 的 UI 设计为“现代深色科技风”。
  - 引入 Material 3 (1.11.0) 和 ConstraintLayout (2.1.4) 依赖，并在 `gradle.properties` 中开启 AndroidX 编译支持。
  - 重定义配色体系，使用太空灰底色、暗蓝灰卡片、紫蓝渐变以及红/黄/蓝/青的等级评分色。
  - 创建了 `CircularScoreView` 自定义环形打分控件，实现分数的动态增长动画及等级色映射。
  - 重新设计未选图状态下的虚线框上传占位图 `selectPlaceholder`。
  - 新增 `item_metric.xml` 子指标可视化卡片布局，使用水平渐变色进度条呈现子项分数。
  - 实现底部说明小贴士的点击折叠/展开效果。
  - 升级 `MainActivity` 继承为 `AppCompatActivity` 并处理了上述所有新 UI 元素的数据绑定与渲染。
- 涉及文件：`gradle.properties`、`app/build.gradle`、`app/src/main/res/values/colors.xml`、`app/src/main/res/values/styles.xml`、`app/src/main/res/drawable/ic_upload.xml`、`app/src/main/res/drawable/ic_tips.xml`、`app/src/main/res/drawable/metric_progress_drawable.xml`、`app/src/main/res/drawable/upload_border_dashed.xml`、`app/src/main/res/drawable/card_background.xml`、`app/src/main/res/drawable/button_gradient_bg.xml`、`app/src/main/res/layout/item_metric.xml`、`app/src/main/res/layout/activity_main.xml`、`app/src/main/java/com/example/imagequality/CircularScoreView.java`、`app/src/main/java/com/example/imagequality/MainActivity.java`。
- 修改原因或目标：优化原有界面简陋、色彩古板、无可视化图表的缺陷，提升为极具科技感与设计感的商业级画质分析工具。
- 验证结果或后续注意事项：已在本地环境中验证通过构建。

## 2026-06-18 11:10:00 +08:00

- 修改内容：将图像分析结果的超长子维度指标详情重构为“双标签页（Tab）虚拟分段页面”。
  - 新建 `tab_layout_bg.xml` 与 `tab_item_selected_bg.xml` 资源，实现圆角、边框、以及紫蓝渐变色高亮的现代分段选择器（Segmented Control）样式。
  - 在 `activity_main.xml` 中将结果区划分为 `overviewLayout`（包括综合评分与评估说明）和 `detailsLayout`（包括子维度详情列表和小贴士卡片），并加入 `tabLayout`。
  - 在 `MainActivity.java` 中绑定 `tabLayout`、`overviewLayout` 和 `detailsLayout` 等双 Tab 控件，添加点击切换监听器；并在图片加载中、分析成功、分析失败等不同状态下动态管理 Tab 及其页面的可见性，每次导入新图片时默认切回第一页“基本结果”。
- 涉及文件：
  - [tab_layout_bg.xml](file:///D:/Users/Desktop/APP/app/src/main/res/drawable/tab_layout_bg.xml) (NEW)
  - [tab_item_selected_bg.xml](file:///D:/Users/Desktop/APP/app/src/main/res/drawable/tab_item_selected_bg.xml) (NEW)
  - [activity_main.xml](file:///D:/Users/Desktop/APP/app/src/main/res/layout/activity_main.xml) (MODIFY)
  - [MainActivity.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/MainActivity.java) (MODIFY)
- 修改原因或目标：解决原先分析结果出来后页面太长、子维度指标详情直接堆叠在下方导致需要大量滚动的体验痛点，提升小屏幕设备上的可用性和视觉清晰度。
- 验证结果或后续注意事项：使用 Gradle 成功编译 Debug APK，打包无任何报错。

## 2026-06-18 11:23:00 +08:00

- 修改内容：新增“色彩丰富度 / 艳丽度 (Colorfulness)”与“细节丰富度 / 信息熵 (Entropy)”两个全新的图像质量评估维度。
  - 在 `ScoreUtils.java` 中将 `weightedOverall` 权重求和公式升级为 7 维版本（清晰度: 0.25, 曝光度: 0.20, 噪点: 0.15, 对比度: 0.15, 色偏: 0.10, 色彩丰富度: 0.08, 信息熵: 0.07）。
  - 在 `QualityAnalyzer.java` 中利用 Hasler & Suesstrunk 对立色空间算法（rg-yb 差值）计算色彩艳丽度，利用直方图 Shannon 熵公式计算细节丰富度，并将这两个指标归一化打分；编写相应的文本诊断机制并动态添加到 `metrics` 列表中。
- 涉及文件：
  - [ScoreUtils.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/ScoreUtils.java) (MODIFY)
  - [QualityAnalyzer.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/QualityAnalyzer.java) (MODIFY)
- 修改原因或目标：丰富画质分析软件的图像质量评估手段，提供对于黑白/褪色/纯色图的智能色彩识别，以及对复杂/单调场景的细节描述能力。
- 验证结果或后续注意事项：使用 Gradle 成功编译 Debug APK，编译通过无任何报错。

## 2026-06-29 12:00:00 +08:00

- 修改内容：将测试图集展示功能重构为“动态对比测试台”，满足自定义真实图片和撰写对比分析的要求，并新增各项画质指标详情的展开查看功能。
  - 删除废弃的内置 Python 生成测试样本，减小 APK 体积。
  - 在 `activity_test_gallery.xml` 顶部增加“添加测试照片”按钮，并加入空状态提示。
  - 改造 `TestGalleryActivity.java`，调用原生 `Intent.ACTION_OPEN_DOCUMENT` 并通过 `EXTRA_ALLOW_MULTIPLE` 支持相册多图批量勾选加载。
  - 选中图片后自动加入列表，并在后台多线程中解析 `Uri` 获取 `Bitmap`，调用现有的 `QualityAnalyzer` 计算并刷新真实得分。
  - 响应“点击进入看指标和单个测试的那种一样”的需求，去除了列表内的下拉展开设计。现在点击“测试图集”里的任意一张测试图卡片，会直接将其 `Uri` 传回主页 (`MainActivity`) 并自动跳转打开，复用主页最完整的详情指标 UI 页面，体验与单测完全一致。
  - 响应“测试图集现在放的这个位置感觉一般”的反馈，将原先挤在顶部右上角的紫色小按钮，移动到了主页中央“请导入图片...”文字的下方，改为一个宽敞的“进入动态对比测试图集”卡片式深色按钮，视觉上更加居中协调、不突兀。
  - 响应“看不到多张照片对比联系”的需求，开发了终极的横向对比大招——**“生成横向对比报告”**。
  - 在“动态对比测试图集”页面的右上角新增了一个绿色的“📊 横向对比报告”按钮。
  - 创建了全新的 `CompareReportActivity`，它能自动抓取当前列表中所有照片的打分数据，生成一张非常专业的**“多列数据对比表格”**。
  - **【最新进展】响应“在App下面加个反例报告”的需求**：为了实现答辩时“脱稿演示”，我在 `CompareReportActivity` 生成的数据大表下方，原生内置了一张精美的 **“💡 典型反例深入分析”** 卡片。它详细描述了 Laplacian 算法被景深虚化照片误导的技术原理（梯度平均化与显著性目标检测缺失）。
- 涉及文件：
  - [activity_test_gallery.xml](file:///D:/Users/Desktop/APP/app/src/main/res/layout/activity_test_gallery.xml) (MODIFY)
  - [item_test_sample.xml](file:///D:/Users/Desktop/APP/app/src/main/res/layout/item_test_sample.xml) (MODIFY)
  - [TestSample.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/TestSample.java) (MODIFY)
  - [TestGalleryAdapter.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/TestGalleryAdapter.java) (MODIFY)
  - [TestGalleryActivity.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/TestGalleryActivity.java) (MODIFY)
  - [activity_main.xml](file:///D:/Users/Desktop/APP/app/src/main/res/layout/activity_main.xml) (MODIFY)
  - [MainActivity.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/MainActivity.java) (MODIFY)
  - [activity_compare_report.xml](file:///D:/Users/Desktop/APP/app/src/main/res/layout/activity_compare_report.xml) (MODIFY)
  - [CompareReportActivity.java](file:///D:/Users/Desktop/APP/app/src/main/java/com/example/imagequality/CompareReportActivity.java) (MODIFY)
  - [AndroidManifest.xml](file:///D:/Users/Desktop/APP/app/src/main/AndroidManifest.xml) (MODIFY)
- 修改原因或目标：极大增强演示的连贯性。现在验收第四点（反例原因分析）时，直接在 App 内向下滑动即可向老师展示详实的技术原因，免去了切出 App 去读文档的麻烦。
- 验证结果或后续注意事项：使用 Gradle 成功编译 Debug APK（`assembleDebug`），无报错。运行后在横向对比报告大表下方可见反例分析卡片。




